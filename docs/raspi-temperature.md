# Measure and act on temparature

All my cases have fans. On the Argon One V2 it can be controlled.

- `vcgencmd measure_temp` for current temparature.
- Argon One: `argonone-config` to set fan speed
